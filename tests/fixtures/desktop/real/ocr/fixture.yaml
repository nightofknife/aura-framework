fixture_schema_version: 2
id: real/ocr
desktop_profile: real
side_effects: true
markers: [desktop, slow]
expected:
  actions:
    - action: plans/aura_base/find_text
      backend: paddleocr
      ok: true
  diagnostics:
    opt_in: true
    command: python cli.py fixture run real/ocr --allow-side-effects
