# -*- coding: utf-8 -*-
"""Aura 核心框架 (`aura_core`)。

`aura_core` 是 Aura 自动化框架的核心包，提供了所有基础功能和构建模块。
它包含了调度器、事件总线、插件管理、状态管理、执行引擎等关键组件，
是构建和运行自动化方案（Plans）和动作（Actions）的基础。

此包旨在提供一个稳定、可扩展的平台，让开发者可以专注于业务逻辑的实现，
而无需关心底层的调度和执行细节。

主要导出模块:
- `Scheduler`: 核心调度器，负责任务的生命周期管理。
- `PluginManager`: 插件管理器，负责加载和管理所有插件。
- `EventBus`: 事件总线，用于系统内的解耦通信。
- `Engine`: 执行引擎，负责实际运行任务图。
- `logger`: 全局日志记录器。
"""